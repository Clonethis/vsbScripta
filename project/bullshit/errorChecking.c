#include<assert.h>
// file for checking all possible errors
// assert -> does nothing if expression false then terminate program
// --> removing assert with cc(1) on compile time : 'gdb -DNDEBUG

// if all users in game are in users file -